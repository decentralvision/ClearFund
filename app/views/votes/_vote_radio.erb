		<% if proposal.active_vote?(user) %>
			<input type="submit" value="Vote" style="color:green" id="<%=proposal.id%>">
			<%= hidden_field_tag "proposal_id", proposal.id %>
		<% else %>
			<input type="submit" value="Vote" id="<%=proposal.id%>">
			<%= hidden_field_tag "proposal_id", proposal.id %>
		<% end %>